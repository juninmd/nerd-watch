{
  "name": "nerd-watch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build:client && bun --watch src/server/index.ts",
    "start": "bun run build:client && bun src/server/index.ts",
    "build:client": "bun run scripts/build-client.ts",
    "lint": "biome lint --error-on-warnings",
    "lint:fix": "biome lint --write",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests",
    "check": "bun run lint && bun run typecheck && bun run test",
    "smoke": "bun run scripts/smoke.ts",
    "seed": "bun run src/server/seed.ts",
    "telegram:login": "bun run scripts/telegram-login.ts"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "plyr": "^3.8.4",
    "qrcode": "^1.5.4",
    "teleproto": "^1.229.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/bun": "latest",
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.7.2"
  }
}
